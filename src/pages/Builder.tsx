export default function Builder() {
  return (
    <div className="max-w-7xl mx-auto px-6 py-16 text-center">
      <h1
        className="text-4xl font-bold tracking-wider mb-3"
        style={{ fontFamily: 'Rajdhani, sans-serif', color: 'var(--color-text)' }}
      >
        Build Simulator
      </h1>
      <p style={{ color: 'var(--color-muted)' }}>Coming in Phase 3 — relic input & stat aggregation.</p>
    </div>
  );
}
